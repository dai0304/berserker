// -*- coding: utf-8; mode: groovy -*-

version = currentVersion

apply plugin: "java"

sourceCompatibility = 1.8
targetCompatibility = 1.8

// ======== libraries ========
repositories {
	mavenCentral()
}

dependencies {
	// spring
	compile "org.springframework:spring-context-support:$springVersion"
}

// ======== wrapper ========
task wrapper(type: Wrapper) {
	gradleVersion = "2.12"
}

// ======== run ========
task execute1(type: JavaExec, dependsOn: classes) {
	main = "jp.classmethod.example.part1.Main"
	classpath = sourceSets.main.runtimeClasspath
}

task execute2(type: JavaExec, dependsOn: classes) {
	main = "jp.classmethod.example.part1.SpringMain"
	classpath = sourceSets.main.runtimeClasspath
}
